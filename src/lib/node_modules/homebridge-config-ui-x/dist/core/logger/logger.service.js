"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const color = require("bash-color");
class Logger {
    log(args) {
        console.log(color.white(`[${new Date().toLocaleString()}]`), color.cyan(`[homebridge-config-ui-x]`), args);
    }
    error(args) {
        console.error(color.white(`[${new Date().toLocaleString()}]`), color.cyan(`[homebridge-config-ui-x]`), color.red(args));
    }
    warn(args) {
        console.warn(color.white(`[${new Date().toLocaleString()}]`), color.cyan(`[homebridge-config-ui-x]`), color.yellow(args));
    }
    debug(args) {
        console.debug(color.white(`[${new Date().toLocaleString()}]`), color.cyan(`[homebridge-config-ui-x]`), args);
    }
    verbose(args) {
        console.debug(color.white(`[${new Date().toLocaleString()}]`), color.cyan(`[homebridge-config-ui-x]`), args);
    }
}
exports.Logger = Logger;
//# sourceMappingURL=logger.service.js.map