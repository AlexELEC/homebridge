import { LinuxService } from './linux.service';
export declare class LinuxController {
    private readonly linuxServer;
    constructor(linuxServer: LinuxService);
    restartHost(res: any): void;
    shutdownHost(res: any): void;
}
