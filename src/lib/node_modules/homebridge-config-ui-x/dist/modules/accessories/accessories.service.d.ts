import { ConfigService } from '../../core/config/config.service';
import { Logger } from '../../core/logger/logger.service';
export declare class AccessoriesService {
    private readonly configService;
    private readonly logger;
    private hapClient;
    constructor(configService: ConfigService, logger: Logger);
    connect(client: any): Promise<void>;
    private refreshrefreshCharacteristics;
    private loadAccessories;
    getAccessoryLayout(username: string): Promise<any>;
    saveAccessoryLayout(user: string, layout: object): Promise<object>;
}
